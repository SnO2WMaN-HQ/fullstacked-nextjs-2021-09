mutation Signup(
  $email: String!
  $alias: String!
  $password: String!
  $displayName: String
) {
  createTempUser(
    email: $email
    alias: $alias
    password: $password
    displayName: $displayName
  ) {
    token
    expiredAt
  }
}
